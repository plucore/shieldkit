# Screenshots

Capture per the spec in `../SUBMISSION-CHECKLIST.md`. Save as `01-hero.png` through `06-plan-switcher.png`. 1600×900 PNG.

Keep this directory's other contents `.gitignore`d if they're large drafts; only the final 6 are committed.

Once captured, upload to Partner Dashboard manually — Shopify's listing UI doesn't pull from git.
